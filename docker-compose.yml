version: '3'
services:
  web:
    image: nginx
    volumes:
      - ./templates:/etc/nginx/templates

    ports:
      - "8089:85"

    restart: always

    environment:
      - NGINX_HOST=kaikay.com
      - NGINX_PORT=85

